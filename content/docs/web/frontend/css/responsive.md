### **第三部分：响应式设计**

1.  **媒体查询 (Media Queries): 适应不同屏幕**
    *   语法：`@media (min-width: ...)` 和 `(max-width: ...)`。
    *   移动优先 (Mobile-First) vs. 桌面优先 (Desktop-First) 策略。

2.  **流式单位 (Fluid Units): 让元素“活”起来**
    *   相对单位：`%`, `vw`, `vh`, `em`, `rem`。
    *   `clamp()` 函数：在特定范围内动态调整大小。